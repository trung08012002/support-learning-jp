import React from "react"

type SearchTextByHandProps = {
  show: boolean
}

const SearchTextByHand = ({ show }: SearchTextByHandProps) => {

  return (
    <div className="w-full h-[340px] relative">

    </div>
  )
};

export default SearchTextByHand;
